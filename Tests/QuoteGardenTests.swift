//
//  QuoteGardenTests.swift
//  QuoteGardenTests
//
//  Created by Master Family on 03/10/2020.
//

import XCTest
@testable import QuoteGarden

class QuoteGardenTests: XCTestCase {

    override func setUpWithError() throws {
        // Put setup code here. This method is called before the invocation of each test method in the class.

    }

    override func tearDownWithError() throws {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }
    
    // MARK: - Tests

    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.

//        let quote = Quote(id: "test", quoteText: "This is a test", quoteAuthor: "Nikola Franičević", quoteGenre: "test")
//        var quoteCD = QuoteCD()
//
//        quoteCD.quoteText?.append(quote.quoteText)
//        quoteCD.quoteGenre?.append(quote.quoteGenre)
//        quoteCD.quoteAuthor?.append(quote.quoteAuthor)
//
//        XCTAssertEqual(quoteCD.quoteText, quote.quoteText)
//        XCTAssertEqual(quoteCD.quoteAuthor, quote.quoteAuthor)
//        XCTAssertEqual(quoteCD.quoteGenre, quote.quoteGenre)
//        XCTAssertEqual(quoteCD.id, quote.id)
        
    }

    func testPerformanceExample() throws {
        // This is an example of a performance test case.
        self.measure {
            // Put the code you want to measure the time of here.
        }
    }

}
